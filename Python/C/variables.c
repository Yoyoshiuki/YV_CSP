//This is a comment in C
#include <stdio.h> //lets us put inputs and outputs

int main(){
    int number = 12;
    float pi = 3.14;
    char = name[] = "Xavier";
    char = person[50];
    int = age;

    printf("Tell me your name!");
    scanf("%s", %person);

    printf("Tell me how old you are\n");
    scanf("%d", &age);

    printf("name is %s and age is %d\n", person, age);

    printf("%f ", pi); // \n makes it so it prints on a new line!
    printf("%d", number);
    return 0; //always the last line of main
}
